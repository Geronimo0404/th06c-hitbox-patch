帮我做一个补丁：把旧版美化包里的「判定点可见」自机贴图，移植进 **Steam《东方紅魔郷》原典版（Classic）**，
让原典版在保留原味画面的同时显示判定点。全程在我本机的正版游戏上操作，**不要**把游戏数据上传到任何地方。

## 你要处理的两份东西（格式完全不同，别混）

- 目标（要改的）：Steam 原典版目录里的 `th06CM.dat`（约 873,760 字节），容器格式 **PKGL**（文件头魔数 `PKGL`）。
- 素材（只读）：旧版 32 位红魔乡的 `美化版 判定点可见\th06c_CM.DAT`（约 1,150,887 字节），
  容器格式 **PBG3**（魔数 `PBG3`），里面是 PNG 贴图。

**直接改名覆盖是无效的**（新引擎第一步校验 `PKGL` 魔数，会直接读不了档）。
要做的是"把同一套美术按新格式重新编码"：抓住素材里的 PNG，合成新版需要的 DDS，
按 PKGL 的加密/压缩规则重新打包回 `th06CM.dat`。

## 已逆向好的格式知识（直接用，不用重新逆）

### 1) PKGL 容器

```
偏移 0   4   "PKGL"
偏移 4   4   u32 directory_size（目录表字节数）
偏移 8   n   目录表：用「档级密钥」做 16 字节周期 XOR，相位 0 在文件偏移 8 处
...          payload 区：每条目用「自己的密钥」做 16 字节周期 XOR，相位 0 在该条目的起始偏移处
末尾        多 1 个 0x00 字节
```

目录记录 = 32 字节固定字段 + 名字（**没有**终止符）：

| 偏移 | 类型 | 含义 |
| --- | --- | --- |
| 0 | u16 | flags，bit0 = payload 是 zstd 帧 |
| 2 | u32 | seed（该条目 payload 的 XOR 密钥种子） |
| 6 | u64 | size（解压后大小） |
| 14 | u64 | stored_size（档内存储大小） |
| 22 | u64 | offset（绝对偏移，**必须 16 字节对齐**） |
| 30 | u16 | name_size |
| 32 | n | 名字，`/` 分隔，条目**按名字升序**排列 |

密钥派生（全程按 64 位取模）：

```c
mix64(x) { x ^= x>>30; x *= 0xbf58476d1ce4e5b9; x ^= x>>27; x *= 0x94d049bb133111eb; return x ^ (x>>31); }
key_from_seed(seed) {
    golden = 0x9e3779b97f4a7c15;
    state = (uint64)seed * 0x9e3779b1 + 1;
    state ^= (uint64)seed << 32;
    key[0..7]  = mix64(state + golden);
    key[8..15] = mix64(state + golden * 2);
}
```

- **档级密钥** = `key_from_seed(crc32(档名去掉扩展名))`，例如 `th06CM.dat → crc32("th06CM")`。
  → **输出档名必须仍然叫 `th06CM.dat`，改名游戏就读不了索引。**
- **条目 seed 可以随便填**（游戏只把它当 XOR 密钥用、从不校验）；
  重打包时填一个常数（比如 `0x006f6f70`）完全没问题。
- 目录表长度通常不是 16 的倍数，所以目录与 payload 的密钥相位不同 —— 按上面写的"各自从自己起始位置算相位 0"处理。

### 2) 贴图：Steam 原典版用的是 DDS

原典版容器里是 `player00.dds` / `player01.dds`（分别是灵梦 / 魔理沙），格式：

```
128 字节 DDS 头 + 256×256×4 字节像素
头字段：magic "DDS "、size=124、flags=0x1000f、height=width=256、pitch=1024、
        pixelformat: flags=0x41、fourCC=0、bits=32、R=0x00ff0000、G=0x0000ff00、B=0x000000ff、A=0xff000000、caps=0x1000
像素字节序：BGRA（即 D3D 的 A8R8G8B8）
```

像素内容 = **RGB 取自 `X.png`，alpha 取自 `X_a.png` 的 R 通道**（旧版是把主图和 `_a` 蒙版分开存的，新引擎合并成一张 RGBA）。

**最省事的做法**：把游戏里现有的 `player00.dds` / `player01.dds` 解出来，
**原样沿用它们的 128 字节头**，只替换后面 262,144 字节的像素。

### 3) 旧 PBG3 档怎么读

`PBG3` 头是位打包的：`magic(4B) + 位读取的 u32(条目数) + 位读取的 u32(表偏移)`；
表里每项 = 5 个 u32（`u1, u2, extra, offset, size`）+ 名字（8 bit 字符、`0` 结尾）；
数据段是 **LZSS**：位流 MSB-first，`flag=1` 读 8 bit 字面量，
`flag=0` 读 13 bit 偏移 + 4 bit 长度（实际长度 = 值 + 3），字典 0x2000 环形；
某条目的压缩数据长度 = 下一个条目的 `offset` − 本条目的 `offset`。

**建议直接找现成实现**（比手写快）：THTK 的 `thdat`（`thdat -x` 能解 th06 的 PBG3），
或任何 th06 PBG3 读取器。我们只需要里面 4 个文件：
`player00.png`、`player00_a.png`、`player01.png`、`player01_a.png`（都是 256×256）。

### 4) 推荐工具：thdat-nc（PKGL 解包/重打包）

GitHub 上的 `Renko6626/thdat-nc` 已经把 PKGL 摸透了，仓库里带
`bin/windows-x86_64/thdat-nc.exe`（zstd 已静态链接，免 DLL），比自己写 zstd 容器省事：

```
thdat-nc.exe -l th06CM.dat                     # 列条目
thdat-nc.exe -x -C outdir th06CM.dat           # 全部解包（会产出 player00.dds 等 28 个文件）
thdat-nc.exe -c outdir/th06CM.dat srcdir       # 重打包（输出名必须保持 th06CM.dat）
```

它重打包时条目 seed 用常数、会把 payload 按 16 字节对齐、末尾补 1 个 0 字节，
与游戏加载器兼容（我们已经在 Steam 原典版实机验证过：能正常进游戏、判定点正常显示）。
**如果你想自己实现**，按上面第 1 节的格式知识写即可，效果一样。

## 执行步骤（请按顺序做，每步都验证）

**0. 备份**
- 先把游戏目录里的 `th06CM.dat` 复制一份出来（建议带时间戳），并记录 SHA256。后面所有改动都能靠它回滚。

**1. 先做"零风险自检"（关键一步，别跳过）**
- 从旧版素材里另外拿一套**原版**（"原版 判定点不可见"）的 `player00.png` + `player00_a.png`，
  按上面的规则合成 DDS，然后和游戏里现有的 `player00.dds` **逐字节比较，必须完全一致**。
- 这一步通过了，说明你对"字节序 / alpha 来源 / DDS 头"的理解全对，后面才敢动手；
  如果对不上，先别继续，逐通道排查（先只比 RGB，再比 A；怀疑是不是 BGR/RGB 搞反了、alpha 取错通道）。

**2. 抽素材**
- 从 `美化版 判定点可见\th06c_CM.DAT` 里解出 `player00.png`、`player00_a.png`、`player01.png`、`player01_a.png`。
- 注意：该美化包的"判定点"是画在**主图 PNG 的 RGB** 上的（`_a` 蒙版与"判定点不可见"版完全相同），
  所以 alpha 还是取 `_a` 的 R 通道。

**3. 解包目标容器并替换贴图**
- 用 `thdat-nc.exe -x` 把游戏目录的 `th06CM.dat` 解包到工作目录；你会看到 28 个条目。
- 用第 2 步的素材合成新的 `player00.dds` / `player01.dds`（沿用原 DDS 的 128 字节头）。
- **其它 26 个文件一个字都不要动。**

**4. 重打包**
- 用 `thdat-nc.exe -c <输出路径>/th06CM.dat <工作目录>` 重新打包。
- 输出文件名必须是 `th06CM.dat`，否则档级密钥对不上。

**5. 回读校验**
- 把刚打出来的档再解包一次，与工作目录**逐文件比对，必须全部一致**。
- 另外确认：只有 `player00.dds` / `player01.dds` 与备份里的原版不同，其余条目字节完全一致。

**6. 安装 + 实机验证**
- 关掉游戏 → 把新档覆盖到游戏目录的 `th06CM.dat`。
- 启动游戏（**必须 Steam 在运行**，这是原典版本身的要求）。
- 验证：进游戏后自机身上应出现红色判定点；最快的看法是停在标题画面不动，
  十几秒后会播 attract demo，demo 中的自机也带判定点。
- 灵梦（player00）和魔理沙（player01）都要各看一眼，确认两张都换了。

**7. 出问题就回滚**
- 用第 0 步的备份覆盖回去，并把游戏目录里 `log.txt` 的内容读出来分析。

## 硬性要求

- **绝对不要**把游戏里的任何数据（原始的或改过的 `.dat`、exe、贴图）上传/发布到网上；
  这个任务只是给我自己本机做 mod。
- 不要改档名、不要改其它 `.dat`、不要动游戏目录里其它文件。
- 每次写回前先备份；报告时给出文件大小、SHA256 和"其余条目未变"的证据。
- 如果游戏正在运行，先关掉再覆盖文件。
- 不要求我保持 Steam 版游戏版本一致，但如果你发现容器结构与我描述的（28 个条目、
  `player00.dds` 等）不一致，先停下来把实际情况报告给我，不要硬套。

## 交付

1. 改好的 `th06CM.dat` + 原版备份（含 SHA256）；
2. 简短说明：你怎么做的、验证结果（重建 DDS 是否逐字节一致、回读校验是否通过、实机是否看到判定点）；
3. 回滚方法（一句话即可）。